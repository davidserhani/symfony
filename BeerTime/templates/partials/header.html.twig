
<body>

<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
    <h5 class="my-0 mr-md-auto font-weight-normal"><a class="navbar-brand text-warning" href="{{ path('home') }}"><span id="emoji">🍻</span>BeerTime</a></h5>
    <form class="form-inline mr-md-auto" method="get" action="{{ path('event_list') }}">
        <input class="form-control mr-sm-2" type="search" name="search" placeholder="Type an event" aria-label="Search">
        <button class="btn btn-outline-warning my-2 my-sm-0" type="submit">Find🔮</button>
    </form>
    <nav class="my-2 my-md-0 mr-md-3">
        <a class="p-2 text-dark" href="{{ path('event_list') }}">Events</a>
        <a class="p-2 text-dark" href="{{ path('event_create') }}">Create</a>
        <a class="p-2 text-dark" href="{{ path('event_random') }}">I'm so thirsty</a>
    </nav>
    <a class="btn btn-outline-success" href="#">Sign up</a>
</div>
<div class="container">